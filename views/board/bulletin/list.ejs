<div class="container mt-5">
    <h2>커뮤니티 게시판</h2>
    <p>다양한 주제에 대해 이야기 나눠보세요!</p>

    <!-- 카테고리 탭 -->
    <div class="d-flex justify-content-between align-items-center">
        <ul class="nav nav-tabs" id="myTab" role="tablist">
            <li class="nav-item" role="presentation">
                <a class="nav-link active" href="/admin/board/brd_key">전체</a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link active" href="/admin/board/brd_key?cat_id=1">Category 1</a>
            </li>
            <li class="nav-item" role="presentation">
                <a class="nav-link active" href="/admin/board/brd_key?cat_id=2">Category 2</a>
            </li>
        </ul>

        <!-- 게시글 작성 버튼 -->
        <div class="ms-auto">
            <a href="<%=path%>/write" class="btn btn-primary">게시글 작성</a>
        </div>
    </div>


    <!-- 게시판 테이블 -->
    <table class="table table-striped table-hover">
        <thead class="table-dark">
        <tr>
            <th scope="col">#</th>
            <th scope="col">제목</th>
            <th scope="col">작성자</th>
            <th scope="col">작성일</th>
            <th scope="col">조회수</th>
        </tr>
        </thead>
        <tbody>
        <% content.posts.forEach((post) => { %>
            <tr>
                <th scope="row">1</th>
                <td><a href="/admin/post/<%= post.post_id %>"><%= post.post_title %></a></td>
                <td>홍길동</td>
                <td><%= post.post_display_date %></td>
                <td>123</td>
            </tr>
        <% }); %>
        <!-- 더 많은 게시글이 여기에 추가될 수 있습니다 -->
        </tbody>
    </table>

    <% if(content.paging.prevShow || content.paging.nextShow) { %>
        <!-- 페이지네이션 -->
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">
                <% if(content.paging.prevShow) { %>
                    <li class="page-item">
                        <a class="page-link" href="/admin/board/brd_key?page=<%= content.paging.start - 1; %>" tabindex="-1"
                           aria-disabled="true">이전</a>
                    </li>
                <% } %>
                <% for(i = content.paging.start; i <= content.paging.end; i++){ %>
                <li class="page-item <%=content.paging.now==i ? 'active' : ''%>"><a class="page-link" href="<%=content.paging.now%>"><%=i%></a></li>
                <% } %>
                <% if(content.paging.nextShow) { %>
                    <li class="page-item">
                        <a class="page-link" href="/admin/board/brd_key?page=<%= contentpaging.end + 1 %>">다음</a>
                    </li>
                <% } %>
            </ul>
        </nav>
    <% } %>
</div>
